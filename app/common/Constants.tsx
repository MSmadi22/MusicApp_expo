import React from 'react';
import { Dimensions } from 'react-native';

const WIDTH = Dimensions.get('screen').width;
const HEIGHT = Dimensions.get('screen').height;



export default{
    cardsWidth:WIDTH*0.45,
    cardsHeight:250,
}
